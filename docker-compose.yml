# docker-compose.yml

services:
  knx-garage-wled:
    build: .
    image: knx-garage-wled:latest
    container_name: knx-garage-wled
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
